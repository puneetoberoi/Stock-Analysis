name: Test Workflow

on:
  push:
    branches: [ main, master, Stable-till-LLMs-agree-but-not-intelligent-till-this-point ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "Hello World"
